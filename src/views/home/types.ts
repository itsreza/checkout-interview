export type OrderDetailTypes = {
  nationalId?: string;
  phoneNumber?: string;
  addressId?: string;
};

export type OrderDetailErrorsTypes = {
  nationalId?: string;
  phoneNumber?: string;
  addressId?: string;
};

export type AddressItemTypes = { name: string; id: string; details: string };
